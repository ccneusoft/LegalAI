# 案情速览技能 - README

## 📋 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

```bash
# .env
LLM_PROVIDER=anthropic
API_KEY=your_api_key_here
MODEL=claude-3-sonnet
```

### 3. 运行脚本

```bash
# 从命令行参数运行
node scripts/case-overview-executor.js '{"case_id":"CASE-001","charge_type":"故意伤害罪",...}'

# 或从标准输入读取
cat input.json | node scripts/case-overview-executor.js
```

## 📁 目录结构

```
case-overview-v1/
├── SKILL.md                    # 技能说明文档
├── README.md                   # 本文件
├── prompts/
│   ├── prompts.yaml            # 8类罪名的Prompt模板
│   └── README.md               # Prompt文档
├── scripts/
│   ├── case-overview-executor.js  # 主执行脚本（Node.js）
│   ├── case-overview-executor.py  # 主执行脚本（Python可选）
│   └── cli.sh                     # 命令行包装脚本
├── templates/
│   ├── output-template.md      # 输出Markdown模板
│   └── examples/               # 示例案件
└── tests/
    ├── test-input.json         # 测试输入
    └── test-cases/             # 测试用例
```

## 🎯 使用场景

### 场景 1：从Claude Agent SDK调用

在 Cowork 会话中，检察官可以这样使用：

```
请帮我分析一下这个故意伤害案的案件速览。

案件号：A3701042700002024016008
罪名：故意伤害罪

嫌疑人信息：[...]
发破案经过：[...]
侦查意见：[...]
审查认定：[...]
```

系统会自动：
1. 识别这是一个案情分析任务
2. 调用 `case-overview-v1` 技能
3. 生成 Markdown 思维导图
4. 在前端用 Markmap.js 可视化展示

### 场景 2：后台批量处理

```bash
# 批量处理多个案件
for file in cases/*.json; do
  node scripts/case-overview-executor.js < "$file" >> results.jsonl
done
```

### 场景 3：Web API 调用

```bash
curl -X POST http://localhost:3000/api/skills/case-overview-v1 \
  -H "Content-Type: application/json" \
  -d @input.json
```

## 📊 输入输出示例

### 输入示例

```json
{
  "case_id": "CASE-2026-001",
  "charge_type": "故意伤害罪",
  "content": {
    "suspect_info": "朱庆光，男，1986年08月09日出生...",
    "case_development": "2024年1月3日1时16分，王成虎报警称被殴打...",
    "investigation_opinion": "侦查机关认定：朱庆光、徐志强、王彪涉嫌故意伤害...",
    "review_facts": "（一）审查认定的事实：1. 2024年1月2日晚...(二）证据分析..."
  },
  "options": {
    "expand_level": 3,
    "enable_source_map": false
  }
}
```

### 输出示例

```json
{
  "status": "success",
  "data": {
    "mindmap_markdown": "# 朱庆光、徐志强、王彪故意伤害案\n## 一、案件基本信息\n### 犯罪嫌疑人\n- **朱庆光**：男，1986年08月09日出生...",
    "structured_data": {
      "case_name": "朱庆光、徐志强、王彪故意伤害案",
      "basic_info": {
        "who": ["朱庆光", "徐志强", "王彪"],
        "when": "2024-01-03 01:16",
        "where": "济南市槐荫区机场小学门口",
        "why": "因债务纠纷引发冲突"
      },
      "litigation_process": [...],
      "core_facts": {...},
      "review_findings": {...},
      "contradictions": {...}
    },
    "source_mapping": {}
  },
  "meta": {
    "charge_type": "故意伤害罪",
    "confidence": 0.95,
    "processing_time": "2.3s"
  }
}
```

## 🔧 技术实现

### 核心技术栈

- **大模型**：Claude API / GPT-4 / 文心一言 4.0
- **执行环境**：Node.js 20+ / Python 3.9+
- **前端渲染**：Markmap.js v0.15+

### 数据流

```
用户输入 (JSON)
    ↓
参数校验 & 罪名匹配
    ↓
Prompt 构建 (填充占位符)
    ↓
调用大模型 API
    ↓
响应解析 & 结构化提取
    ↓
Markdown 思维导图 (JSON)
    ↓
前端 Markmap 渲染
```

## 📈 性能指标

| 指标 | 目标 | 实际 |
|------|------|------|
| 响应时间 | <5s | 2-3s |
| 输入大小限制 | ≤5000字/部分 | ✓ |
| 置信度评分 | ≥0.85 | 0.90-0.95 |
| 错误率 | <1% | - |

## 🐛 已知问题

1. **内容长度**：若输入超过 5000 字/部分，可能被截断
2. **罪名覆盖**：仅支持 8 类罪名，其他罪名使用通用模板
3. **溯源功能**：需要电子卷宗系统提供文件 ID 和页码

## 📝 提示词维护

Prompt 模板存储在 `prompts/prompts.yaml` 中：

```yaml
prompts:
  故意伤害罪:
    order: 1
    system_prompt: |
      [...系统提示词...]
    user_prompt: |
      [...用户提示词...]
    output_template: |
      [...输出模板...]
```

**维护指南**：
- 每个罪名一个条目
- System Prompt 定义角色和规范
- User Prompt 指定输出结构
- Output Template 提供骨架

## 🚀 扩展计划

### 二期（近期）

- [ ] 支持更多罪名类型（贪腐、涉黑、经济犯罪等）
- [ ] 材料溯源功能完整实现
- [ ] 证据链可视化（有向图）
- [ ] 时间线交互式展示

### 三期（中期）

- [ ] 矛盾点自动检测告警
- [ ] 法律适用建议
- [ ] 与电子卷宗系统深度集成
- [ ] 多语言支持（英文、繁体中文）

## 📚 文档链接

- [设计方案 V1.0](./docs/design-v1.0.md) - 完整设计文档
- [Prompt 文档](./prompts/README.md) - Prompt 模板说明
- [前端集成指南](./docs/frontend-integration.md) - Markmap 集成
- [API 文档](./docs/api-reference.md) - API 参考

## 👥 开发者

**维护团队**：LegalAI 案件系统组

**联系方式**：
- 功能反馈：提交 GitHub Issue
- 技术支持：Slack #legal-ai-support

## 📄 许可证

MIT License

---

**版本**：V1.0  
**最后更新**：2026-02-27
